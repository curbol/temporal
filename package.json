{
  "name": "temporal-keyboard",
  "version": "1.0.0",
  "description": "Temporal keyboard ergogen configuration",
  "scripts": {
    "gen": "cd ergogen && ergogen . -o output",
    "convert": "mkdir -p cases && cd ergogen/output/cases && for f in *.jscad; do npx @jscad/cli@1 \"$f\" -of stla -o $INIT_CWD/cases/$(basename \"$f\" .jscad).stl; done"
  },
  "devDependencies": {
    "ergogen": "^4.1.0",
    "@jscad/cli": "^1.10.0"
  }
}
